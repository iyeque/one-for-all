document.addEventListener('DOMContentLoaded', () => { const stats = document.getElementById('stats'); chrome.runtime.sendMessage({ action: 'checkStatus' }, (response) => { if (response && response.status === 'online') { stats.textContent = 'AdGuard Home is Connected.'; stats.style.color = 'green'; } else { stats.textContent = 'AdGuard Home is Offline.'; stats.style.color = 'red'; } }); });